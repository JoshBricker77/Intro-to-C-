#include <iostream>
using namespace std;

int main() {
    int x = 2; // Initialize x with the value 2
    int squared = x * x; // Square x by multiplying it with itself

    // Print the result
    cout << "The square of " << x << " is " << squared << "." << endl;

    return 0; // Indicate that the program ended successfully
}